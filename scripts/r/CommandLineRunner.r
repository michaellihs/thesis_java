#! /usr/bin/env Rscript

# For further information on scripting with r see
# http://cran.r-project.org/doc/manuals/R-intro.html#Scripting-with-R

# Make this script executeable and call it via ./ScriptName.R
# You can add arbitrary command line arguments which will then be read via
args <- commandArgs(TRUE)

# Print arguments to command line with the following command
args

# Quit execution of r
q()